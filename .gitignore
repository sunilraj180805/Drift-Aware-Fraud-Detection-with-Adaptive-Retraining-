# Dataset (large, downloaded separately from Kaggle — not included in repo)
elliptic_bitcoin_dataset/

# Note: results/ (figures, tables, CSVs) is intentionally tracked, not ignored,
# so results are viewable on GitHub without re-running the notebook.

# Jupyter
.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
venv/
.venv/

# OS
.DS_Store
